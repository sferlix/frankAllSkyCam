This folder holds frankAllSkyCam's runtime logs (capture, watchdog, timelapse,
startrail, retention, ephemeris and crontab job output), plus alive.txt used
by the watchdog to detect a stalled capture loop.

On a fresh install this folder is seeded empty; log files appear here once
the cron jobs start running. Safe to clear if it grows too large.
